<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.MemberDAO">

<insert id="signUp" parameterType="com.example.demo.dto.MemberDTO">
    INSERT INTO member (
        id,
        name,
        password,
        email,
        phone,
        address,
        gender,
        sns_token,
        role
    ) 
    VALUES (
        #{id},
        #{name},
        #{password},
        #{email},
        #{phone},
        #{address},
        #{gender},
        #{snsToken},
        #{role}
    )
</insert>

<select id="checkId" parameterType="String" resultType="int">
SELECT COALESCE(COUNT(*), 0) FROM member WHERE id = #{id}
</select>

</mapper>
